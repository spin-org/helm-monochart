{{- $enabled_configmap := .Values.configmap.enabled }}

{{- if $enabled_configmap }}
  {{- range $configmap := .Values.configmap.configs }}
---
apiVersion: v1
data:
  {{ $configmap.filename }}: |
    {{ $configmap.file_content }}
kind: ConfigMap
metadata:
  name: {{ $configmap.name }}
  namespace: {{ $configmap.namespace }}
  labels:
    name: {{ $configmap.name }}
  {{- end }}
{{- end }}
